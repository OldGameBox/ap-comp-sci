#java #booleans
- A **Boolean** refers to a value that is either `true` or `false`.
- It is one of the [[primitive types]] in Java.
- **Naming Convention**: Boolean variables are typically named like questions (e.g., `isStudent`, `hasPassed`) to clearly indicate their purpose.
- You can declare, initialize, and print them using [[System.out.println()]].