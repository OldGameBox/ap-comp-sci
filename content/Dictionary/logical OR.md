#java #logic
- Represented by two vertical lines (`||`), often called "pipes."
- Results in `true` if either the left side, the right side, or both sides are `true`. It only evaluates to `false` if both operands are `false`.