#java #logic
- A feature in Java where the second argument of a [[logical AND]] or [[logical OR]] expression is only evaluated if the first argument is not sufficient to determine the value of the expression.
- This is often used to guard against potentially dangerous operations (like division by zero) that appear later in the same expression.