#java
- Inputs to a [[Method]] that allow you to pass data into it.
- Defined in the method header with a type and a name (e.g., `int length`).
- Allow a single method to behave differently based on the input provided.