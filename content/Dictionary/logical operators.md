#java #logic
- Symbols used to connect two or more [[boolean]] expressions or to flip the value of a single expression.
- The three primary operators in Java are [[logical AND]] (`&&`), [[logical OR]] (`||`), and [[logical NOT]] (`!`).