#java
- Variables declared with `static final` (and often `private`) that hold a fixed value that does not change during the program's execution.
- In the example, `Min` and `Max` were used as constants to define the loop range.