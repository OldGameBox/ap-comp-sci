#java
- A statement using the `return` keyword that stops the execution of a [[Method]] and provides the resulting value to the code that called it.
- Must match the **[[return types]]** defined in the method signature.