#java #strings
- A method used to compare two [[String]] objects to see if they have the same content.
- Syntax: `string1.equals(string2)`
- It returns a [[boolean]] value (`true` if they match, `false` if they don't).