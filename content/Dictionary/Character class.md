#java 
- A class in Java that provides useful static methods for working with **[[char]]** data.
- Unlike the primitive `char`, the `Character` class is an object type and contains methods like `isLetter()` and `toLowerCase()`.